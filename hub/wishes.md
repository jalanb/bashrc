# Wishes

If we could afford these features
    That'd be great

# sibh

\jalanb
```bash
j<Tab>
```

That shows many commands, including "java".
- It shouldn't

I'd like better control over what appears after I hit <Tab> in the shell.
I'd like to be able to offer good suggestions after I hit <Tab> even on an empty line.
/
\alan

See ./sibh.md, which expands on this

# Usage charts

I surely have many functions in this project which I never use.
If you look through my history, you'll find them used once or twice straight after defined, but never later.

Would be nice to get a list of "rarely used functions" and consider deleting them.

# Pythonisation

Would be nice to have all these `bash` functions available in `python` / `ipython` too.
