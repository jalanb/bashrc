	>>> import environ 

A method to read the keys set in ~/.jab/environ
	>>> keys = environ.read_environ_keys()
	>>> show( keys[ 5 : 15 ] )
	['AMG_SOURCE',
	 'AMG_TRUNK',
	 'BUILDER_WORKING',
	 'BUILDING',
	 'BUILD_DEBUG',
	 'EDITOR',
	 'EXITSTATUS',
	 'HISTSIZ',
	 'JABS',
	 'LCP']

Turn those keys into a dictionary with values from bash's environment
	>>> bash = environ.read_bash(keys)
	>>> bash['JABS'].endswith('jab')
	True
	>>> bash['EDITOR'].endswith('/vim')
	True

Bash's environment variables are attributes of the module
	>>> print environ.JABS == bash['JABS']
	True
	>>> print environ.EDITOR == bash['EDITOR']
	True
