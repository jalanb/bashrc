ly
==

	>>> import ly
	>>> from path import cd
	>>> cd(ly.__file__)
	True


ly is a script
	>>> ly.argv.test_args('-h ')
	Usage: try.py [options]
	<BLANKLINE>
	Options:
	  -y, --ython     hide python scripts
	  -t, --tests     show tests
	  -c, --compiled  show compiled python
	  -g, --grammar   show grammars
	  -f, --fails     show fails
	  -a, --all       show all python related files
	  -o, --others    show non python related files as well
	  -q, --quiet     hide directories, blank lines, etc
	  -l, --lines     separate by returns (tabs is default)
	  -i, --ignore    Do not ignore files (that subversion ignores)
	  -h, --help      show this help message and exit

The command line tells it which files to get
	>>> ly.argv.test_args('-a ')

	>>> import l

filter_names
------------

Set up
	>>> makepath('trace.log').write_text('hello')
	>>> names = l.get_names()

This method removes all names
	which do not have at least one wanted file
	A file's extension decides whether it is wanted
	For example, the ly module does not want log files

	>>> 'ly' in names, 'trace' in names
	(True, True)
	>>> ly.filter_names(names)
	>>> 'ly' in names, 'trace' in names
	(True, False)

Tear down
	>>> makepath('trace.log').remove()
