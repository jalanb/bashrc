The last_fm module
==================
	>>> import last_fm

The module provides a method to initialize a session
	>>> session = last_fm.init()

The session allows us to call various last.fm method
	Such as "artist.getInfo"

	>>> info = session.read('artist.getInfo', artist='Pink Floyd')
	>>> print info.values()[0]['url']
	http://www.last.fm/music/Pink+Floyd

	>>> number_of_tracks, tracks = last_fm.my_recent_tracks(session)
	>>> show(tracks)

