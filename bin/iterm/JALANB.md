This dir has scripts that are called for iTerm2 profiles

There is a bash script per AI
- Each of these affords an environment to its AI
- and then calls "in_projects.sh" with a bash command to start that AI 
