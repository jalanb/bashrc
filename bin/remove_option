#! /usr/bin/env python3
"""Remove an option from args"""

import re

from pysyte.cli.main import run


def add_args(parser):
    parser.string('option')
    parser.string('string')


def main(args):
    result = re.sub(args.option, '', args.string)
    print('' if result == '-' else result)
    return True

run(main, add_args)
