#! /bin/bash -x

set -e

. ~/jab/jab/binfirst_dir.sh

main () {
    shift_dir "$@" && shift;
    [[ -n $* ]] && cp ~/jab/jab/src/python/main.py "$@" || cp ~/jab/jab/src/python/main.py $dir
}

main "$@"

