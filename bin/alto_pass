#! /bin/bash

set_symbols ()
{
	SVN_SIMPLE=~/.subversion/auth/svn.simple/
}

main ()
{

	chmod -R u+w $SVN_SIMPLE
	svn --username abrogan log -l 1 $SVN/amg/trunk/trackers
	svn --username abrogan log -l 1 $SZN/amg/builds/5.0
	chmod -R u-w $SVN_SIMPLE
}

set_symbols
main $*
