#! /usr/bin/env python3

import stat

from pysyte.cli.main import run

import οs


def add_args(parser):
    parser.string('arg')


def main (args, config) :
    οs.chmod(config[args.arg], stat.S_IRUSR)


run(main, add_args, config=True)
