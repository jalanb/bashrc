#! /bin/bash

# all alan"s aliases
# 	as ordered by strcmp()

# One quote might help in reading these files:
# 	"If in doubt, call it Fred", Dr. Mike Scott, Dublin City University, late 80s

# This list is quite long enough, we accept no other aliases
#	Thanks anyway
unalias -a 

# The wee x headings are counts
# 	all the wee aliases in this wee section have only one character, oui,
#		Awwww.
#   There is an alias for every letter
#		albeit most are unused

# x

alias -- -="c -"
alias 1="v $JAB_TODO"
alias 2="python2.7 $JAB/python/todo.py"
alias 3="tree"
alias 7="svn"
alias a="ack"
alias b="b"
alias c="kd "
alias d="d"
alias e="ex --noplugin -u ~/.vim/exrc"
alias f="find "
alias g="grep --color"
alias h="history | tail -n 25"
alias i="i"
alias j="j"
alias k="k"
alias l="$LS_PROGRAM "
alias m="m"
alias n="n"
alias o="o"
# p is a function
alias q="exit 0"
alias Q="exit 0"
alias r="rm -vrf"
alias s="s"
alias t="t"
alias u="u"
alias v="$JAB/bin/fim "
alias w="which "
alias x="x"
alias y='clear; p $JAB/python/y.py '
alias z="du -sh"

# xx

alias ..='c ..'

alias 3l="tree | less"

alias aa="ack --all"
alias ac="ack --code"
alias ae="ack --erl"
alias ap="ack --python"
alias at="ack --pyt"
alias av="vack"
alias aw="ack -w"

alias cy="c \$PYTHON_HOME"

alias eg="env | g "

alias gc="git commit -m"
alias gh="history | g "

alias hh="history -n"

alias IP="python $JAB/python/ifconfig.py 172"

alias jj="c $JAB"

alias kp="rsync -a"

alias l1="ls -1tr "
alias la="ls -a "
alias ld="ls -1d"
alias lj="ls $JAB"
alias ll="ls -lh "
alias lr="ls -lhtr"
alias ls="$LS_PROGRAM $LS_COLOUR_OPTION $LS_DIRS_OPTION"
alias lt="ls *.test*"
alias lx="p $JAB/python/ls/lx.py "
alias ly="p $JAB/python/ls/ly.py "

alias p-="echo $OLDPWD"
alias pc="python -c"
alias pg="pgrep -fl"
alias pi="ipython "
alias pp="p $JAB/python/mython/pp.py"
alias pt="p $JAB/python/vim_traceback.py"

alias r.="rm -rvf *"
alias rr="$JAB/python/ranger/bin/range"
alias rq="rm -rf "
alias ry="rf pycos"

alias sa="source $JAB/aliases"
alias sb="c $JAB && source bashrc && -"
alias sc="svn ci -m"
alias sd="svn diff "
alias se="source $JAB/environ"
alias sf="source $JAB/functons"
alias si="svn_info "
alias sl="svn_log "
alias so="sort "
alias sp="source $JAB/prompt"
alias sr="svn resolved "
alias sv="svn revert "
alias st="stat "

alias tf="tail -f"

alias v.="v ."
alias va="v $JAB/aliases; sa"
alias vb="v $JAB/bashrc; sb"
alias ve="v $JAB/environ; se"
alias vf="v $JAB/functons; sf"
alias vh="v ~/.bash_history +"
alias vj="v $JAB"
alias vp="v $JAB/prompt; sp"
alias vt="v -p *.txt"
alias vv="v $JAB/bin/fim"
alias vy="v $PYTHON_HOME"

alias xk="exchange_keys"

alias yy="y $PYTHON_HOME"

alias z.="zm \*"

# xxx

alias ...='c ../..'

alias 7up='svn up'

alias aaa="ack --nojunk"
alias aav="vack --all"
alias acv="vack --code"
alias aev="vack --erl"
alias apv="vack --python"
alias atv="vack --pyt"
alias awv="vack -w"


alias doc="c ~/Documents"

alias clx="cl; lx "
alias cly="cl; ly "
alias csi='c $(python -c "import sys; print [p for p in sys.path if p.endswith(\"site-packages\")][0]")'

alias jjy="ky $JAB/python"
alias jlf="sst jlfieblu jlf.ie"

alias kpj="kp -e \"ssh -i $JAB_KEY\"" 
alias kpp="kp --progress"

alias lda="ls -1da"
alias lff="ls fred*"
alias lra="lr -a"
alias lrd="lr -d"
alias lsh="ls *.sh"

alias out="to_release"

alias ps3="ps axf | vim - +"

alias rq.="/bin/rm -rf ./*"
alias rrs="sudo $JAB/python/ranger/bin/range"
alias rff="r fred* *.tmp"

alias sib=". ~/.bashrc"
alias sii="svn_issue "

alias scd='svn diff --diff-cmd colordiff '
alias shd='svn_diff horizontal '
alias sla="source $JAB/local/aliases"
alias sle="source $JAB/local/environ"
alias slf="source $JAB/local/functons"
alias sli="svn_log_issue"
alias slp="source $JAB/local/prompt"
alias spe="source $JAB/python-environ"
alias sth="statvim shd"
alias stj="statvim svd $JAB"
alias stm="statvim shd | grep -v ^r"
alias stv="statvim svd "
alias svd="svn_diff vertical "

alias tra="try ~/alans_tests"
alias ttt='ssj abrogan ie-track-1.altobridge.com'

alias vib="v ~/.bashrc; sib"
alias vla="v $JAB/local/aliases; sla"
alias vlb="v $JAB/local/bashrc; slb"
alias vle="v $JAB/local/environ; sle"
alias vlf="v $JAB/local/functons; slf"
alias vlp="v $JAB/local/prompt; slp"
alias vpe="v $JAB/python-environ; spe"
alias vvv="v $JAB/vim"

alias url="python ~/bin/show_url.py "

# xxxx

alias ....='c ../../..'

alias alto="source $JAB/company-environment"
alias aaav="vack --nojunk"

alias mine="sudo chown $(id -un):$(id -gn)"

alias jjya='ky $JAB/python/alanology'
alias jjys='ky $JAB/python/site'
alias junk="mount ie-serv-2.altobridge.com:/export/junk /Users/shared/alto/junk"

alias rias="sst abrogan 172.20.1.172"

alias tags="p $JAB/python/ptags.py "

alias vvvp="v $JAB/vim/ftplugin/python"
alias vvvv="v $JAB/vim/vimrc"
alias vvvy="v $JAB/vim/ftplugin/python/jab.vim"

# xxxxx

alias .....='c ../../../..'

alias ptags="p $JAB/python/ptags.py "

# xxxxxx

alias ......='c ../../../../..'

alias lessen="less -N"


# xxxxxxx

alias .......='c ../../../../../..'
alias service="sudo /sbin/service "
alias cleaner="clean deep"

# xxxxxxxx

alias cleanest="clean deep pycos"

# xxxxxxxx

alias stopwatch="killall -9 watch"

alias update_hosts="sudo python $JAB/python/update_hosts.py"
