#! /bin/cat

If you have any of these back them up now

	rm -rf ~/.jab
	rm -rf ~/.vim
	rm -rf ~/.vimrc

Go $HOME
	
	cd

Check out from the main branch of the repository

	svn co http://intranet.altobridge.com/jab .jab

Add links where programs expect to find them

	ln -s ~/.jab/vim ~/.vim
	ln -s ~/.jab/vim/vimrc ~/.vimrc
	ln -s ~/.jab/python/pythonrc.py ~/.pythonrc.py
	ln -s ~/.jab/etc/subversion/config ~/.subversion/config
	ln -s ~/.jab/etc/dir_colors ~/.dir_colors
 
Start using it

	echo "test -f ~/.jab/bashrc && cd ~/.jab && . bashrc && cd >/dev/null 2>&1" >> ~/.bashrc

