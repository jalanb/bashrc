The surname module
==================

    >>> import surname
    >>> assert 'pretty names' in surname.__doc__

Prettifiying strings
====================

    >>> fred = surname.pretty('fred')
    >>> assert fred != 'fred'
    >>> assert surname.plain(fred) == 'fred'
