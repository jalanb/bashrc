The οs module
==================

    >>> import οs
    >>> assert 'nice names' in οs.__doc__

Prettifiying strings
====================

    >>> fred = οs.nice('fred')
    >>> assert fred != 'fred'
    >>> assert οs.name(fred) == 'fred', f"fred={fred!r} != 'fred'"
