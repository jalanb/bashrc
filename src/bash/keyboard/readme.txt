Collection of smaller commands

These comands are particularly important as they are most frequently used.

Aim: For a key "x" there should be commands "x", "xx", "xxx"

In order to gain a script in this package a letter must provide at least the first three, but may have more.

A script can contain similar commands as long as they start with (if x is the letter) "[_]*x"
